# headway (CLI)

Command-line binary for [Headway](../README.md), wrapping
[`headway-core`](../core) with argument parsing, interactive prompts,
output rendering, and exit-code contract.

This crate builds the `headway` executable. See the
[top-level README](../README.md) for installation, subcommand usage, and
exit codes.
