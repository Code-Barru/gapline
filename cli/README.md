# gapline (CLI)

Command-line binary for [Gapline](../README.md), wrapping
[`gapline-core`](../core) with argument parsing, interactive prompts,
output rendering, and exit-code contract.

This crate builds the `gapline` executable. See the
[top-level README](../README.md) for installation, subcommand usage, and
exit codes.
